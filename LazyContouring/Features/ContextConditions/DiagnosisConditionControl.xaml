<UserControl x:Class="LazyContouring.UI.Views.ContextConditionControls.DiagnosisConditionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LazyContouring.UI.Views.ContextConditionControls" 
             xmlns:contextconditions="clr-namespace:LazyContouring.Operations.ContextConditions" 
             xmlns:contextconditions1="clr-namespace:LazyContouring.UI.ViewModels.Operations.ContextConditions"
             d:DataContext="{d:DesignInstance Type=contextconditions1:DiagnosisConditionVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding Title}" Margin="2"/>
        <TextBox Text="{Binding DiagnosisCondition.CodeShouldStartsWith}" Margin="2"/>
        <CheckBox IsChecked="{Binding DiagnosisCondition.ShouldBe}"  Content="Should be" Margin="2"/>
    </StackPanel>
</UserControl>
