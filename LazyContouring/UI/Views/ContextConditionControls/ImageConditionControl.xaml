<UserControl x:Class="LazyContouring.UI.Views.ContextConditionControls.ImageConditionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LazyContouring.UI.Views.ContextConditionControls" xmlns:contextconditions="clr-namespace:LazyContouring.UI.ViewModels.Operations.ContextConditions" d:DataContext="{d:DesignInstance Type=contextconditions:ImageConditionVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="2">
            <TextBlock Text="{Binding Title}"/>
            <TextBox Text="{Binding ImageCondition.SliceThickness}" MinWidth="30" Margin="5 0 0 0"/>
            <TextBlock Text=" mm"/>
        </StackPanel>

        <CheckBox IsChecked="{Binding ImageCondition.ShouldBe}"  Content="Should be" Margin="2"/>
        
    </StackPanel>
</UserControl>
