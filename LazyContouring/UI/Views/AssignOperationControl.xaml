<UserControl x:Class="LazyContouring.UI.Views.AssignOperationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LazyContouring.UI.Views" 
             xmlns:viewmodels="clr-namespace:LazyContouring.UI.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:AssignOperationVM}"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="100">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding StructureVariableVM.StructureId}"/>
        <TextBlock Text=" = " Background="#222" Foreground="White" FontWeight="Bold" Margin="5 0 0 0"/>
    </StackPanel>
</UserControl>
