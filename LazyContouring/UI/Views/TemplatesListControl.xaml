<UserControl x:Class="LazyContouring.UI.Views.TemplatesListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LazyContouring.UI.Views" 
             xmlns:operations="clr-namespace:LazyContouring.UI.ViewModels.Operations" xmlns:viewmodels="clr-namespace:LazyContouring.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:MainVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Templates:" Margin="3"/>
        <TabControl Grid.Row="1">
            <TabItem Header="Manual" Content="{Binding TemplateManagerVM.TemplateManager.ManualTemplates}"/>
            <TabItem Header="Automatic" Content="{Binding TemplateManagerVM.TemplateManager.ManualTemplates}"/>
        </TabControl>

    </Grid>
</UserControl>
