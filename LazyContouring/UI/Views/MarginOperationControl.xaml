<UserControl x:Class="LazyContouring.UI.Views.MarginOperationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LazyContouring.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="36"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Column="0" Grid.RowSpan="2" Source="/LazyContouring.esapi;component/Images/MarginOperation.png" Width="30"/>

        <TextBlock Grid.Column="1" Grid.Row="0" Text="Margin, mm" Margin="3"/>

        <TextBox Grid.Column="2" Grid.Row="0" Text="{Binding MarginInMM}" Margin="3"/>
    </Grid>
</UserControl>
