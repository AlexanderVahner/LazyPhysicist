<UserControl x:Class="LazyContouring.UI.Views.CropOperationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LazyContouring.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="36"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Column="0" Grid.RowSpan="2" Source="{Binding BitmapImage}"/>
        <RadioButton Grid.Column="1" Grid.Row="0" Content="Inside" IsChecked="{Binding InsideChecked}" Margin="3"/>
        <RadioButton Grid.Column="1" Grid.Row="1" Content="Outside" IsChecked="{Binding OutsideChecked}" Margin="3"/>
        <TextBlock Grid.Column="2" Grid.Row="0" Text="Margin, mm" Margin="3"/>
        <TextBox Grid.Column="2" Grid.Row="1" Text="{Binding CropOperation.MarginInMM}" Margin="3" MinWidth="20"/>        
    </Grid>
</UserControl>
