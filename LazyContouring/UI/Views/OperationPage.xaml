<Page x:Class="LazyContouring.UI.Views.OperationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LazyContouring.UI.Views" 
      xmlns:viewmodels="clr-namespace:LazyContouring.UI.ViewModels" 
      d:DataContext="{d:DesignInstance Type=viewmodels:OperationsVM}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OperationPage">

    <Grid x:Name="MainGrid">
        <ListBox ItemsSource="{Binding Operations}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:OperationStringControl DataContext="{Binding}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
